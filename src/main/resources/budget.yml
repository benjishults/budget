persistence:
  type: JDBC
  jdbc:
    driver: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/budget
    budgetName: just playin'
    #    database: budget
    schema: scratch
    #    dbProvider: postgresql
    #    port: 5432
    host: localhost
    user: budget
    password: budget
  file:
    dataDirectory: ~/.data/bps-budget

persistence:
  type: JDBC
  jdbc:
    driver: org.postgresql.Driver
    schema: test
    user: test
    password: test
    budgetName: test with basic accounts
    host: localhost
  file:
    dataDirectory: hasGeneralWalletAndFood
